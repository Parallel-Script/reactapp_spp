Jenkinsfile
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                   sh 'echo "Build completed completed"'
                }
            }
        }
    }
}
